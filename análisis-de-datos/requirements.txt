# === Web framework ===
Flask==3.0.3
waitress==3.0.0            # servidor de produccion multiplataforma (opcional)
python-dotenv==1.0.1       # carga de .env

# === HTTP / scraping ===
requests==2.32.3
httpx==0.27.2              # cliente async por si se paraleliza con cuidado

# === Datos y modelos de probabilidad ===
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1              # distribuciones de Poisson, ajuste Dixon-Coles
soccerdata==1.8.7          # xG y stats profundos (FBref, Sofascore) sin API key

# === Persistencia ===
SQLAlchemy==2.0.31         # ORM sobre SQLite (incluido en Python)

# === IA opcional (contexto cualitativo / redaccion de alertas) ===
# Elegir UNO segun el proveedor. Todos tienen free tier o son locales.
groq==0.9.0               # free tier rapido
# google-generativeai==0.7.2   # Gemini free tier (descomentar si se usa)
# ollama==0.3.1                # modelos locales, coste cero (descomentar si se usa)

# === Utilidades ===
pydantic==2.8.2            # validacion de request/response
tenacity==8.5.0            # reintentos con backoff ante rate limits

# === Testing ===
pytest==8.2.2
